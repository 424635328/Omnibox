# 🚀 Omnibox

> **A Blazing Fast, Cross-Platform Productivity Launcher.**
>
> 极速、智能、现代化的跨平台启动器，基于 Rust 和 Tauri 构建。

![Rust](https://img.shields.io/badge/Rust-Backend-orange?style=flat-square&logo=rust)
![Vue 3](https://img.shields.io/badge/Vue_3-Frontend-42b883?style=flat-square&logo=vue.js)
![Tauri](https://img.shields.io/badge/Tauri-v1.5-blue?style=flat-square&logo=tauri)
![License](https://img.shields.io/badge/license-MIT-green?style=flat-square)

**Omnibox** 旨在替代传统的系统菜单，通过全局快捷键 `Alt + Space` 瞬间唤醒，提供毫秒级的文件搜索与应用启动体验。它不仅快，还能**学习你的使用习惯**，越用越顺手。

---

## ✨ 核心特性 (Features)

### ⚡ 极致性能 (Performance)
*   **Rust 驱动**: 后端核心逻辑完全由 Rust 编写，确保极高的执行效率与内存安全。
*   **并行全盘扫描**: 摒弃单线程，采用 `jwalk` + `rayon` 架构，充分利用多核 CPU 并行扫描全盘文件。
*   **智能剪枝**: 自动识别并跳过 `node_modules`, `.git`, `Windows` 等黑洞目录，扫描速度提升 10 倍。
*   **零延迟启动**: 采用 `bincode` 二进制序列化缓存索引，冷启动时间 <100ms。

### 🧠 智能排序算法 (Smart Ranking)
*   **混合搜索策略**:
    *   **模糊匹配**: 基于 `skim` 算法，支持拼写错误容忍。
    *   **全名匹配**: 完美支持带扩展名的精确搜索（如 `report.pdf`, `6.15.pptx`）。
*   **习惯记忆 (Contextual Habits)**:
    *   系统会自动记录 *"当搜索 'c' 时，用户选择了 Chrome"*。
    *   下次搜索 'c'，Chrome 将获得极高权重并置顶。
*   **频次加权**: 根据全局使用频率动态调整排名。

### 🎨 现代交互体验 (Modern UX)
*   **极简 UI**: 仅保留搜索框与结果列表，支持 Acrylic 毛玻璃背景特效。
*   **交互优化**:
    *   **滚轮防抖**: 智能识别滚动行为，防止列表滚动时鼠标误触导致选中项跳动。
    *   **键盘优先**: 全键盘操作流 (`↑` `↓` 选择，`Enter` 打开，`Esc` 隐藏)。
*   **设置面板**: 内置可视化设置，支持修改最大结果数、一键配置开机自启。

### 🛠 系统集成
*   **全局快捷键**: 默认 `Alt + Space` 唤醒/隐藏。
*   **系统托盘**: 支持后台常驻与快速退出。
*   **文件类型识别**: 自动识别并标记 Application, PDF, Image, Script 等文件类型。

---

## 🏗 技术栈 (Tech Stack)

项目采用 **前后端分离** 架构，通过 Tauri IPC 进行高效通信。

| 模块 | 技术选型 | 关键库/工具 | 作用 |
| :--- | :--- | :--- | :--- |
| **Backend** | **Rust** | `tauri` | 核心业务逻辑、系统 API 调用 |
| | | `jwalk` / `rayon` | 高性能并行文件系统扫描 |
| | | `fuzzy-matcher` | 字符串模糊匹配算法 |
| | | `bincode` / `serde` | 高效二进制数据序列化与存储 |
| | | `tauri-plugin-autostart` | 跨平台开机自启管理 |
| **Frontend** | **Vue 3** | `Composition API` | 用户界面逻辑与状态管理 |
| | **TypeScript** | | 类型安全与代码健壮性 |
| | **Vite** | | 极速构建与 HMR 热更新 |
| | **UI** | `lucide-vue-next` | 现代矢量图标库 |

---

## 📂 目录结构

```text
Omnibox/
├── backend/            # Rust 后端 (Tauri Core)
│   ├── src/
│   │   ├── main.rs     # 主程序入口、命令注册、窗口管理
│   │   ├── scanner.rs  # 多线程文件扫描器 (黑名单/白名单逻辑)
│   │   ├── models.rs   # 数据结构定义 (SearchResult, UserHabits)
│   │   └── storage.rs  # 持久化存储层 (Binary File I/O)
│   ├── Cargo.toml      # Rust 依赖管理
│   └── tauri.conf.json # Tauri 配置文件
│
├── frontend/           # Vue 3 前端
│   ├── src/
│   │   ├── App.vue     # 核心 UI 组件 (搜索、列表、设置)
│   │   └── main.ts     # Vue 入口
│   ├── vite.config.ts  # Vite 配置
│   └── package.json    # 前端依赖管理
│
└── README.md           # 项目说明文档
```

---

## 🚀 快速开始 (Getting Started)

### 前置要求
*   **Rust**: 建议安装最新 Stable 版本。
*   **Node.js**:用于构建前端资源。
*   **Build Tools**: Windows 用户需安装 C++ 生成工具 (Visual Studio Build Tools)。

### 1. 开发环境运行 (Development)

你需要打开两个终端窗口：

**终端 A (前端):**
```bash
cd frontend
npm install
npm run dev
# 前端服务将运行在 http://localhost:1420
```

**终端 B (后端):**
```bash
cd backend
cargo run
# Tauri 将编译 Rust 代码并启动应用窗口
```

### 2. 生产环境打包 (Build)

**Step 1: 构建前端资源**
```bash
cd frontend
npm run build
# 确保生成了 dist 目录
```

**Step 2: 打包应用程序**
```bash
cd backend
cargo tauri build
```
打包完成后，安装包将位于 `backend/target/release/bundle/msi` (Windows) 目录下。

---

## ❓ 常见问题 (FAQ)

**Q: 为什么搜索不到某些文件？**
A:
1.  **扫描尚未完成**: 首次启动或手动刷新索引时，全盘扫描需要几秒钟（取决于硬盘速度）。
2.  **黑名单限制**: 检查文件是否位于 `node_modules`, `target`, `Windows` 等被自动忽略的目录中。
3.  **扩展名过滤**: 目前仅索引 `.exe`, `.lnk`, `.pdf` 等常用格式，可在 `scanner.rs` 中修改白名单。

**Q: 如何重置所有数据？**
A: 删除用户缓存目录下的 `omnibox` 文件夹。
*   Windows: `%localappdata%/omnibox`

**Q: 滚动条不显示？**
A: 请在设置面板中将 "Max Results" 调大（例如 100）。如果结果数量不足以填满窗口，滚动条会自动隐藏。

---

## 📄 许可证

本项目采用 [MIT License](LICENSE) 开源。